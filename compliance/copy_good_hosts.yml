---
- name: Copy a good version of the hosts file to a server
  gather_facts: false
  hosts: localhost
  connection: local

  tasks:
    - copy:
      src: /var/lib/awx/public/files/good_hosts
      dest: /etc/hosts
      owner: root
      group: root
      mode: 0644
      backup: yes
